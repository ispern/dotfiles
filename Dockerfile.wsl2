# WSL2環境テスト用Dockerfile - Microsoftカーネルをシミュレート
FROM ubuntu:22.04

# 環境変数の設定
ENV DEBIAN_FRONTEND=noninteractive

# 最小限のパッケージのみインストール（install.shが依存するもの）
RUN apt-get update && \
    apt-get install -y \
    curl \
    wget \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# WSL2環境をシミュレートするため、カーネル情報を偽装
RUN echo "5.10.102.1-microsoft-standard-WSL2" > /proc/version 2>/dev/null || true

# 作業ディレクトリの設定
WORKDIR /workspace
